apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "snowflake-id-gen.fullname" . }}-test-connection"
  labels:
    {{- include "snowflake-id-gen.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "snowflake-id-gen.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
